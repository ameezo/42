// type cast

// take a value in a certain type and convert it into another type

// (type) expression
// (type to convert to) the value

#include <stdio.h>

int	main()
{
	int	a = 5;
	int	b = 2;
	printf("%d\n" , a/b);
	printf("%f\n" , (double) a/b);
}
